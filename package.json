{
  "name": "worldgame",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Phaser 4 and TypeScript port of the WorldGame Perlin terrain prototype.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8080 --strictPort",
    "build": "npm run check && vite build",
    "check": "tsc --noEmit",
    "preview": "vite preview --host 0.0.0.0 --port 8080 --strictPort"
  },
  "dependencies": {
    "phaser": "4.0.0"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.4.3"
  }
}
